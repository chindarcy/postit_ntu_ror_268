<!--<h1>Test</h1> -->
<%= render 'share/title', title: "Index page" %>
<% @posts.each do |post|%>
<div class="row">
	<div class="well col-sm-4">
	<p><%= post.content %></p>
	<span>
		<%= link_to "show post", post_path(post)%>
	</span>
  </div>
</div>
<%end%>